version: '3'

services:
  rccremote:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./data:/data
    networks:
      - app-network

  rcc:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./data:/data
    networks:
      - app-network

networks:
  app-network:
    driver: bridge

volumes:
  data-volume:
    driver: local
